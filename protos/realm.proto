import "geometry.proto";

message Layer {
  optional string terrain = 1;
  repeated sint32 tiles = 2;
}

message Region {
  repeated Layer layers = 1;
  repeated uint32 passabilities = 2;
  repeated string entity_ids = 3;
}

message Realm {
  optional string name = 1;
  optional Vector2 size = 2;
}
